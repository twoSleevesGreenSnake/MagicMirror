<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="WelcomeBackTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/welcome_background</item>
    </style>

    <style name="ItemFragmentGoodsNameTv">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ItemFragmentGoodsPriceTv">
        <item name="android:textStyle">bold</item>
    </style>

</resources>
